[debug]
debug = true
devices = []
function = "debug_fun"
sinks = ["web"]

[debug.source]
event = "debug"
id = "web"

[uart_black_ring_detect]
debug = true
devices = ["color_camera"]
function = "black_ring_detect"
sinks = [
    "web",
    "uart",
]

[uart_black_ring_detect.source]
event = "4"
id = "uart"

[uart_color_detect]
debug = true
devices = ["color_camera"]
function = "color_detect"
sinks = [
    "web",
    "uart",
]

[uart_color_detect.source]
event = "1"
id = "uart"

[uart_cross_detect]
debug = true
devices = ["cross_camera"]
function = "cross"
sinks = [
    "web",
    "uart",
]

[uart_cross_detect.source]
event = "3"
id = "uart"

[uart_qr_detect]
debug = true
devices = ["qr_camera"]
function = "qr_detect"
sinks = [
    "web",
    "uart",
]

[uart_qr_detect.source]
event = "2"
id = "uart"
