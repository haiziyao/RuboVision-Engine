# RuboVision 电控通信格式与数据

本文档是电控联调的单页说明，内容与当前 `example` 分支配置一致。正式识别命令和固定结果样例命令都会同时送往 Web Sink 与 UART Sink。

## 1. 通信接口

### 1.1 UART 参数

| 平台 | 串口设备 | 波特率 | 数据格式 |
|---|---|---:|---|
| Ubuntu 开发机 | `/tmp/rubo-uart` | 115200 | 8N1，无流控 |
| 香橙派 | `/dev/ttyAMA1` | 9600 | 8N1，无流控 |
| 树莓派 | `/dev/serial0` | 9600 | 8N1，无流控 |

香橙派使用 3.3V TTL UART：

```text
电控 TX  -> 香橙派 RX（物理针脚 11）
电控 RX  <- 香橙派 TX（物理针脚 36）
电控 GND -- 香橙派 GND（物理针脚 6）
```

必须共地，TX/RX 必须交叉。不得把 5V UART TX 直接接入板卡 RX。

### 1.2 Web 接口

```text
地址：http://<设备IP>:3888
手动触发：POST /api/debug/trigger
最近结果：GET  /api/outputs/latest
调试列表：GET  /api/debug/bindings
```

## 2. UART 请求帧

每条请求固定为 4 字节：

```text
+--------+---------+--------+--------+
| 0x61   | COMMAND | 0x0D   | 0x0A   |
| 'a'    | 1 byte  | CR     | LF     |
+--------+---------+--------+--------+
```

```text
REQUEST = 0x61 + COMMAND + 0x0D + 0x0A
```

`COMMAND` 是一个原始二进制字节，不是 ASCII 数字。例如颜色识别必须发送：

```text
正确：61 01 0D 0A
错误：61 31 0D 0A    // 这是 Debug 命令，不是颜色命令
```

## 3. UART 命令和固定数据

### 3.1 正式识别命令

| 功能 | COMMAND | 完整请求（HEX） | UART 返回格式 |
|---|---:|---|---|
| 颜色识别 | `0x01` | `61 01 0D 0A` | `<color>\n` |
| 二维码识别 | `0x02` | `61 02 0D 0A` | `<qr-content>\n` |
| 同心圆环定位 | `0x03` | `61 03 0D 0A` | `CROSS,0,<found>,<dx>,<dy>,<score>\n` |
| 黑环识别 | `0x04` | `61 04 0D 0A` | `RING,<found>,<dx>,<dy>,<score>\n` |
| A/B/C 字母识别 | `0x05` | `61 05 0D 0A` | `A\n`、`B\n` 或 `C\n` |
| 彩色柱定位 | `0x06` | `61 06 0D 0A` | `BLOCK,<color>,<found>,<dx>,<dy>\n` |

### 3.2 不使用摄像头的结果样例命令

这些命令返回固定成功数据，专门用于验证电控解析器、Web Sink 和 UART Sink。

| 功能样例 | COMMAND | 完整请求（HEX） | 固定 UART 返回 | Web Binding ID |
|---|---:|---|---|---|
| 颜色 | `0x11` | `61 11 0D 0A` | `blue\n` | `uart_sample_color_result` |
| 二维码 | `0x12` | `61 12 0D 0A` | `RUBO-QR-SAMPLE\n` | `uart_sample_qr_result` |
| 同心圆环 | `0x13` | `61 13 0D 0A` | `CROSS,0,1,-12,8,92\n` | `uart_sample_concentric_ring_result` |
| 黑环 | `0x14` | `61 14 0D 0A` | `RING,1,-5,11,88\n` | `uart_sample_black_ring_result` |
| 字母 | `0x15` | `61 15 0D 0A` | `A\n` | `uart_sample_letter_result` |
| 彩色柱 | `0x16` | `61 16 0D 0A` | `BLOCK,blue,1,-20,15\n` | `uart_sample_color_block_result` |

### 3.3 Debug 命令

| 功能 | COMMAND | 完整请求（HEX） | UART 返回 |
|---|---:|---|---|
| 基础通信测试 | `0x31`，即 ASCII `'1'` | `61 31 0D 0A` | `debug success\n` |

## 4. UART 响应通用格式

UART Sink 只取函数 JSON 结果中的 `value` 字段，并追加一个 `LF`：

```text
RESPONSE = UTF-8(value) + 0x0A
```

响应没有帧头、长度、请求编号或校验和，也不保证带 `CR`。电控必须以 `0x0A` 作为一条响应的结束标志，并可兼容删除末尾可选的 `0x0D`。

### 4.1 颜色

```text
red\n
blue\n
green\n
black\n
white\n
```

当前配置颜色名为 `red`、`blue`、`green`、`black`、`white`。未在限定帧数内确认颜色时，函数返回错误文本，不会伪造一个颜色。

### 4.2 二维码

```text
<二维码中的 UTF-8 文本>\n
```

二维码内容长度不固定。用于联调的二维码内容不应包含换行符。

### 4.3 同心圆环

```text
CROSS,0,<found>,<dx>,<dy>,<score>\n
```

示例：

```text
CROSS,0,1,-12,8,92\n
CROSS,0,0,0,0,0\n
```

字段共 6 个：

| 字段 | 类型 | 含义 |
|---|---|---|
| `CROSS` | 固定文本 | 数据类型 |
| `0` | 固定整数 | 当前运行参数，现固定为 0 |
| `found` | `0`/`1` | 是否找到目标 |
| `dx` | `int32` 文本 | 目标相对中心的横向偏差，向右为正 |
| `dy` | `int32` 文本 | 目标相对中心的纵向偏差，向下为正 |
| `score` | `0..100` | 识别评分 |

### 4.4 黑环

```text
RING,<found>,<dx>,<dy>,<score>\n
```

示例：

```text
RING,1,-5,11,88\n
RING,0,0,0,0\n
```

字段共 5 个，含义与同心圆环相同。

### 4.5 字母

```text
A\n
B\n
C\n
```

只接受一个大写字母。识别依据为空洞数：A 为 1 个、B 为 2 个、C 为 0 个。

### 4.6 彩色柱定位

```text
BLOCK,<color>,<found>,<dx>,<dy>\n
```

示例：

```text
BLOCK,blue,1,-20,15\n
BLOCK,red,1,24,-8\n
BLOCK,unknown,0,0,0\n
```

字段共 5 个：

| 字段 | 类型 | 含义 |
|---|---|---|
| `BLOCK` | 固定文本 | 数据类型 |
| `color` | 文本 | 柱子颜色；未找到时为 `unknown` |
| `found` | `0`/`1` | 是否找到柱子 |
| `dx` | `int32` 文本 | 柱子中心相对目标点的横向偏差，向右为正 |
| `dy` | `int32` 文本 | 柱子中心相对目标点的纵向偏差，向下为正 |

### 4.7 错误数据

函数已匹配但执行失败时，可能返回：

```text
Function: <错误信息>\n
Runtime: <错误信息>\n
Dispatch: <错误信息>\n
```

电控应把这些前缀识别为失败，记录完整行，但不得依赖后续英文内容做固定业务分支。无效帧或不存在的命令不保证产生响应。

## 5. Web 触发格式

Web 页面会列出所有 `debug = true` 的 Binding。也可以直接发 HTTP 请求：

```http
POST /api/debug/trigger HTTP/1.1
Content-Type: application/json

{
  "binding_id": "uart_sample_color_block_result",
  "description": "control parser test",
  "payload": {}
}
```

命令行示例：

```bash
curl -X POST http://127.0.0.1:3888/api/debug/trigger \
  -H 'content-type: application/json' \
  -d '{"binding_id":"uart_sample_color_block_result","description":"control parser test","payload":{}}'
```

触发成功的 API 应答：

```json
{
  "ok": true,
  "data": "uart_sample_color_block_result",
  "error": null
}
```

Web Sink 中对应的函数结果数据：

```json
{
  "text": "color_block_detect sample: BLOCK,blue,1,-20,15",
  "value": "BLOCK,blue,1,-20,15",
  "found": true,
  "color": "blue",
  "color_output": "blue",
  "center_x": 300,
  "center_y": 255,
  "dx": -20,
  "dy": 15,
  "area": 25000.0,
  "bbox": { "x": 210, "y": 165, "width": 180, "height": 180 },
  "sample": true
}
```

同一次 Web 触发仍会经过两个 Sink：Web 保存完整 JSON，UART 发送 `value + LF`。

## 6. 电控发送参考

### 6.1 STM32 HAL

```c
typedef enum {
    RUBO_COLOR = 0x01,
    RUBO_QR = 0x02,
    RUBO_CONCENTRIC_RING = 0x03,
    RUBO_BLACK_RING = 0x04,
    RUBO_LETTER = 0x05,
    RUBO_COLOR_BLOCK = 0x06,

    RUBO_SAMPLE_COLOR = 0x11,
    RUBO_SAMPLE_QR = 0x12,
    RUBO_SAMPLE_CONCENTRIC_RING = 0x13,
    RUBO_SAMPLE_BLACK_RING = 0x14,
    RUBO_SAMPLE_LETTER = 0x15,
    RUBO_SAMPLE_COLOR_BLOCK = 0x16,

    RUBO_DEBUG = 0x31,
} RuboCommand;

HAL_StatusTypeDef rubo_send(UART_HandleTypeDef *uart, RuboCommand command)
{
    uint8_t frame[4] = {0x61, (uint8_t)command, 0x0D, 0x0A};
    return HAL_UART_Transmit(uart, frame, sizeof(frame), 100);
}
```

不要使用 `sprintf("a%d\r\n", command)`，它会发送 ASCII 数字而不是原始命令字节。

### 6.2 Python 串口测试

```python
import serial


def request(port: serial.Serial, command: int) -> str:
    port.write(bytes((0x61, command, 0x0D, 0x0A)))
    port.flush()
    response = port.readline()
    if not response.endswith(b"\n"):
        raise TimeoutError("response did not end with LF")
    return response.rstrip(b"\r\n").decode("utf-8")


with serial.Serial("COM10", 9600, timeout=3) as uart:
    print(request(uart, 0x12))  # RUBO-QR-SAMPLE
```

## 7. 时序与解析要求

当前协议没有请求 ID，电控必须保持单请求时序：

```text
发送一帧 -> 等待一行响应或超时 -> 再发送下一帧
```

- 同一时间只能有一条在途请求。
- 不要在未收到响应前连续发送视觉命令。
- 结果样例和 Debug 建议超时 2 秒。
- 颜色、圆环、黑环、字母和彩色柱建议初始超时 15 秒。
- 二维码建议初始超时 30 秒。
- CSV 必须先检查固定前缀和字段数，再转换整数。
- `found` 只能是 `0` 或 `1`，`score` 必须在 `0..100`。
- 接收缓冲区不得假设固定长度，二维码文本和错误文本可能较长。

## 8. GPIO 状态线

GPIO 是函数运行状态辅助信号，不承载 UART/CSV 结果数据。当前配置为低有效：

| 平台 | GPIO chip | 运行状态线 | 附加配置线 |
|---|---:|---:|---|
| 香橙派 | 7 | line 3 | line 4、line 5 |
| 树莓派 | 0 | line 27 | line 17、line 22 |
| Ubuntu | 无 | 无 | 无 |

当前实现会在函数开始时拉为有效、结束时恢复；业务识别结果仍以 UART 返回为准。表中的 line 编号是 GPIO line offset，不是板卡物理针脚号。

## 9. 最小联调顺序

1. 发送 `61 31 0D 0A`，确认收到 `debug success\n`。
2. 发送 `61 11 0D 0A` 至 `61 16 0D 0A`，逐个验证电控解析器。
3. 在 Web 最近结果中确认同一请求也有完整 JSON。
4. 样例命令全部通过后，再测试 `0x01` 至 `0x06` 的真实摄像头识别。

更详细的电气、时序和排错说明见 `doc/uart_control_protocol.md`。
